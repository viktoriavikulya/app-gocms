package views

import (
	platformtempl "github.com/fastygo/platform/pkg/renderers/templ"
	"github.com/fastygo/templ/components/card"
	"github.com/fastygo/templ/ui/button"
)

templ Page(props PageProps) {
	<!DOCTYPE html>
	<html lang="en">
		<head>
			<meta charset="UTF-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1"/>
			<title>{ props.Title }</title>
			<link rel="stylesheet" href="/static/css/app.css"/>
		</head>
		<body class="min-h-screen bg-background text-foreground antialiased">
			<div class="flex min-h-screen">
				<aside class="w-64 border-r border-border bg-muted/20 p-4" aria-label="Product navigation">
					@card.Card(card.CardProps{Variant: "muted", Class: "mb-4"}) {
						@card.CardHeader(card.CardHeaderProps{Class: "space-y-1"}) {
							@card.CardTitle(card.CardTitleProps{Order: 2}, "GoCMS Admin")
							@card.CardDescription(card.CardDescriptionProps{}, "Content workspace")
						}
					}
					<nav class="space-y-1 text-sm" aria-label="CMS resources">
						for _, item := range props.Nav {
							@NavLink(item.Href, item.Label)
						}
					</nav>
				</aside>
				<main class="flex-1 p-6">
					<div class="mx-auto max-w-6xl space-y-6">
						if props.Screen.Metadata != nil && props.Screen.Metadata["taxonomy_type"] != "" {
							<span data-gocms-taxonomy-type={ props.Screen.Metadata["taxonomy_type"] } class="sr-only"></span>
						}
						<header class="space-y-1">
							<p class="text-sm text-muted-foreground">Schema-driven Platform preview</p>
							<h1 class="text-2xl font-semibold tracking-tight">{ props.Screen.Title }</h1>
							<nav class="text-sm text-muted-foreground" aria-label="Breadcrumb">
								<a class="hover:text-foreground" href="/go-admin/">Admin</a>
								<span aria-hidden="true"> / </span>
								<span>{ props.Screen.Title }</span>
							</nav>
						</header>
						@platformtempl.Screen(props.Screen)
					</div>
				</main>
			</div>
		</body>
	</html>
}

templ Home() {
	<!DOCTYPE html>
	<html lang="en">
		<head>
			<meta charset="UTF-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1"/>
			<title>GoCMS Platform Preview</title>
			<link rel="stylesheet" href="/static/css/app.css"/>
		</head>
		<body class="min-h-screen bg-background text-foreground antialiased">
			<main class="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-6 p-6">
				<div class="space-y-3">
					<p class="text-sm font-medium text-muted-foreground">FastyGo Platform</p>
					<h1 class="text-4xl font-semibold tracking-tight">GoCMS Platform Preview</h1>
					<p class="text-muted-foreground">A runnable AppCMS shell for schema-driven CMS admin surfaces.</p>
				</div>
				<div class="flex gap-3">
					<a class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground" href="/go-admin/">Open admin</a>
					<a class="rounded-md border border-border px-4 py-2 text-sm font-medium" href="/go-json/">Open API</a>
				</div>
			</main>
		</body>
	</html>
}

templ NavLink(href string, label string) {
	@button.Button(button.ButtonProps{Variant: "ghost", Href: href, Class: "w-full justify-start text-muted-foreground hover:text-accent-foreground"}) {
		{ label }
	}
}
